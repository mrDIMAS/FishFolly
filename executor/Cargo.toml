[package]
name = "executor"
version = "0.1.0"
edition = "2021"

[dependencies]
fyrox = { path = "../../Fyrox/fyrox" }
fish_fall = { path = "../game", optional = true }

[features]
default = ["fish_fall"]
dylib = []